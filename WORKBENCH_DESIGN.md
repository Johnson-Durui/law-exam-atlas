# 真题关系网工作台设计说明

## 设计判断

- **界面类型**：高密度资料检索工作台。
- **主要任务**：左侧定位真题，中间观察关系，右侧核对题干、来源和关联依据。
- **参考重点**：采用视频抽帧中的三栏比例、低装饰、高信息密度和图内快速预览；不复用其中与法硕无关的工程字段或标识。
- **视觉语言**：白色画布、极细灰边、深灰文字、低饱和绿色选中态。层级主要依靠位置、字号和分隔线，不依靠大卡片与强阴影。
- **关键交互**：工作台常驻目录 / 图谱 / 详情；全景只保留图谱；题库保留长列表与详情。图谱节点可先在右上角快速预览，再打开完整详情。

## 响应式规则

- `>= 1100px`：`240px / 主图 / 310px` 三栏。
- `761–1100px`：`220px / 主图 / 280px` 三栏。
- `<= 760px`：图谱、目录、详情依次纵向排列；目录保留独立滚动区与分页。

## 实现约束

- 保留原有筛选、模式切换、图谱控制、题库、详情、来源对话框的 ID 和脚本加载顺序。
- 新增目录分页节点 `catalog-list`、`catalog-count`、`catalog-prev`、`catalog-next`、`catalog-page`。
- 新增快速预览节点 `peek-card`、`peek-title`、`peek-body`、`peek-close`、`peek-open`。
- 本轮按安全约束只进行静态 HTML / CSS 检查，不启动浏览器、本地服务或 Playwright。
