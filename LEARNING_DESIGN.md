# 关系网内容与交互

对象：零基础法硕（非法学）备考者。打开即进入全窗口的 2027 考向网络，另有真题全网、知识逻辑两个入口。

视觉：白底、黑灰文字与细线；深蓝导航、橙色预测、灰紫科目辅助色。不要绿色、渐变、发光效果或装饰性卡片。科目、考向与审题步骤由实际内容决定结构。

真题全网：保留真实题目节点、候选考点、年份与题型筛选。知识逻辑：五科分别以问题、条件、例外、结论为线索串起审题步骤和知识点。2027 考向：情景 → 分支 → 复习考点 → 真实题目中的候选词语线索。

点击考向直接展开子图和右侧详情，可查看依据、适用边界、审题顺序、原创练习与自查。科目/情景筛选、搜索、返回全览、拖拽、缩放、键盘操作和 SVG 导出都保留。hash 链接可直接分享某条分支。

预测只表示复习假设，没有命题概率和伪回测。相关词语可能来自错误选项；不能将词语匹配视为确定考点。近期证据筛选使用自动提取状态，仍不等于人工逐字校对。

验证：纯模型测试覆盖每条分支与学习路径；最小 DOM/Canvas 测试覆盖页面事件、分支跳转、筛选与图形导出。独立 SVG 转 PNG 用于布局检查；这些验证不冒充真实浏览器截图。公开发布后检查部署状态、资源 HTTP 响应和文件哈希。
